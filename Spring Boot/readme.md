## Spring Boot
```text
SpringBoot是Spring中的框架，用来简化Spring应用程序的创建和开发过程，也可以说SpringBoot可以简化之前采用的SSM框架进行开发的过程。
在采用传统的SSM框架进行开发时，搭建和整个三个框架，需要很多的准备工作。比如配置web.xml，spring.xml，springmvc.xml，mybatis.xml等。
而在SpringBoot中，采用的大量的默认配置简化了开发过程。
```